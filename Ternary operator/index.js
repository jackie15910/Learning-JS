/* condition ? exprIfTrue : exprIfFalse */

let adult = checkAge(11);
console.log(adult);

function matureFn(age){

    return age >= 18 ? true : false
}

/* checkWinner (false);

function checkWinner(win){
    win ? console.log('You WIN!') ; console.log('You LOSE!')
} */

/* if(age >= 18){
    return true;
    }
    else{
        return false;
    } */